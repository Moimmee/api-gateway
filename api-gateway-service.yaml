apiVersion: v1
kind: Service

metadata:
  name: api-gateway-service

spec:
  type: NodePort
  selector:
    app: api-gateway-app
  ports:
    - port: 9090
      targetPort: 8080
      protocol: TCP
      nodePort: 30000